name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  # The crate is warning-free in every build mode. Keep it that way.
  RUSTFLAGS: -Dwarnings

jobs:
  test:
    name: test (${{ matrix.os }}, ${{ matrix.features }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        # The two modes are genuinely different builds, not a formality:
        # `default` compiles the tree-sitter grammars and enables the semantic
        # predicates; `none` exercises the feature-contract path where those
        # predicates must error instead of silently returning nothing.
        features: [default, none]
        include:
          - features: default
            flags: ""
          - features: none
            flags: "--no-default-features"

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          # Feature sets produce different artifacts; don't let them evict
          # each other. The default build compiles 21 grammar crates.
          key: ${{ matrix.features }}

      - name: Install jj
        shell: bash
        env:
          # Pinned deliberately. The integration tests assert on jj's output
          # format and revset behaviour, so an unpinned jj would turn an
          # upstream release into a mystery failure here.
          JJ_VERSION: v0.44.0
        run: |
          set -euo pipefail
          case "${{ runner.os }}-${{ runner.arch }}" in
            Linux-X64)   TARGET=x86_64-unknown-linux-musl ;;
            Linux-ARM64) TARGET=aarch64-unknown-linux-musl ;;
            macOS-ARM64) TARGET=aarch64-apple-darwin ;;
            macOS-X64)   TARGET=x86_64-apple-darwin ;;
            *) echo "unsupported runner: ${{ runner.os }}-${{ runner.arch }}" >&2; exit 1 ;;
          esac
          URL="https://github.com/jj-vcs/jj/releases/download/${JJ_VERSION}/jj-${JJ_VERSION}-${TARGET}.tar.gz"
          echo "installing $URL"
          mkdir -p "$HOME/.local/bin"
          # Members are stored with a leading "./". Extract the whole archive
          # and move the binary out, rather than naming a member -- GNU and
          # BSD tar disagree about whether "jj" matches "./jj".
          TMP="$(mktemp -d)"
          curl -fsSL "$URL" | tar xz -C "$TMP"
          mv "$TMP/jj" "$HOME/.local/bin/jj"
          chmod +x "$HOME/.local/bin/jj"
          rm -rf "$TMP"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Verify jj is the pinned version
        run: jj --version

      - name: Build
        run: cargo build --locked ${{ matrix.flags }}

      - name: Test
        run: cargo test --locked ${{ matrix.flags }}

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    # A gate, as of the fork becoming permanent.
    #
    # This was advisory for a long time, and the reason was real: CI ran on the
    # feature branches too, and those carried upstream's `src/` unchanged, so
    # gating would have failed a branch's own build over code it did not own.
    # The stated trigger was "main itself being clean".
    #
    # Collapsing to a single branch settled it from both ends. `main` is now
    # the only branch CI runs on, and it is clippy-clean -- zero warnings in
    # every feature mode, from the `chore(clippy)` work that cleared all
    # eleven. There is no longer a branch carrying unowned code to protect.
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          key: clippy
      - name: Clippy
        # `-D warnings` after `--` denies clippy's own lints, which RUSTFLAGS
        # alone does not reliably do. The top-level `-Dwarnings` still covers
        # rustc's.
        run: cargo clippy --locked --all-targets -- -D warnings
