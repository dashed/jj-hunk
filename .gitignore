# FP snapshot directory (isolated git repo for workdir snapshots)
.fp/snapshots/


# Added by cargo

/target

# Agent worktrees. Untracked but not ignored, these were swept into a
# `git add -A` and committed as gitlinks inside an integration merge.
.claude/worktrees/
